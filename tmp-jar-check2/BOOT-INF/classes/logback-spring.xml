<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds">

    <!-- 瀹氫箟鍙橀噺 -->
    <property name="LOG_HOME" value="/opt/app/logs" />
    <property name="APP_NAME" value="LeeDaud" />
    <property name="MAX_HISTORY" value="30" />
    <property name="MAX_FILE_SIZE" value="10MB" />
    <property name="TOTAL_SIZE_CAP" value="1GB" />

    <!-- 鎺у埗鍙拌緭鍑猴紙鎵€鏈夌幆澧冮€氱敤锛?-->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{0} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ==================== 鐢熶骇鐜锛氭枃浠?Appender ==================== -->
    <springProfile name="prod">

        <!-- 涓诲簲鐢ㄦ棩蹇楁枃浠?-->
        <appender name="APPLICATION_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/${APP_NAME}-application.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_HOME}/${APP_NAME}-application-%d{yyyy-MM-dd}-%i.log</fileNamePattern>
                <maxHistory>${MAX_HISTORY}</maxHistory>
                <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
                <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{ip:-}] %-5level %logger{0} - %msg%n</pattern>
            </encoder>
        </appender>

        <!-- 閿欒鏃ュ織鍗曠嫭鏂囦欢 -->
        <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/${APP_NAME}-error.log</file>
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>ERROR</level>
            </filter>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_HOME}/${APP_NAME}-error-%d{yyyy-MM-dd}-%i.log</fileNamePattern>
                <maxHistory>30</maxHistory>
                <maxFileSize>10MB</maxFileSize>
                <totalSizeCap>500MB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{ip:-}] %-5level %logger{0} - %msg%n</pattern>
            </encoder>
        </appender>

        <!-- 瀹夊叏鎵弿鏃ュ織鍗曠嫭鏂囦欢 -->
        <appender name="SECURITY_SCAN_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/${APP_NAME}-security-scan.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_HOME}/${APP_NAME}-security-scan-%d{yyyy-MM-dd}-%i.log</fileNamePattern>
                <maxHistory>90</maxHistory>
                <maxFileSize>10MB</maxFileSize>
                <totalSizeCap>2GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{ip:-}] %-5level %logger{0} - %msg%n</pattern>
            </encoder>
        </appender>

        <!-- SQL鎱㈡煡璇㈡棩蹇?-->
        <appender name="SQL_SLOW_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/${APP_NAME}-sql-slow.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_HOME}/${APP_NAME}-sql-slow-%d{yyyy-MM-dd}-%i.log</fileNamePattern>
                <maxHistory>7</maxHistory>
                <maxFileSize>10MB</maxFileSize>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{0} - %msg%n</pattern>
            </encoder>
        </appender>

        <!-- 瀹夊叏鎵弿鏃ュ織鍣?-->
        <logger name="SECURITY_SCAN" level="WARN" additivity="false">
            <appender-ref ref="SECURITY_SCAN_FILE" />
            <appender-ref ref="CONSOLE" />
        </logger>

        <!-- MyBatis SQL鏃ュ織 -->
        <logger name="cc.leedaud.mapper" level="WARN">
            <appender-ref ref="APPLICATION_FILE" />
        </logger>

        <!-- 鎱QL鏃ュ織 -->
        <logger name="cc.leedaud.mapper.slow" level="DEBUG" additivity="false">
            <appender-ref ref="SQL_SLOW_FILE" />
        </logger>

        <!-- 404閿欒澶勭悊 -->
        <logger name="org.springframework.web.servlet.PageNotFound" level="ERROR" additivity="false">
            <appender-ref ref="SECURITY_SCAN_FILE" />
        </logger>

        <!-- 鏍规棩蹇?-->
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="APPLICATION_FILE" />
            <appender-ref ref="ERROR_FILE" />
        </root>

    </springProfile>

    <!-- ==================== 寮€鍙戠幆澧冿細浠呮帶鍒跺彴 ==================== -->
    <springProfile name="dev">

        <!-- 瀹夊叏鎵弿鏃ュ織鍣紙寮€鍙戠幆澧冧粎杈撳嚭鍒版帶鍒跺彴锛?-->
        <logger name="SECURITY_SCAN" level="WARN" additivity="false">
            <appender-ref ref="CONSOLE" />
        </logger>

        <!-- MyBatis SQL鏃ュ織锛堝紑鍙戠幆澧冩樉绀?SQL锛?-->
        <logger name="cc.leedaud.mapper" level="DEBUG" />

        <!-- 404閿欒澶勭悊 -->
        <logger name="org.springframework.web.servlet.PageNotFound" level="ERROR" additivity="false">
            <appender-ref ref="CONSOLE" />
        </logger>

        <!-- 鏍规棩蹇?-->
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>

    </springProfile>

    <!-- ==================== 鎵€鏈夌幆澧冮€氱敤 ==================== -->
    <logger name="org.apache.tomcat" level="ERROR" />
    <logger name="org.apache.coyote" level="ERROR" />
    <logger name="org.apache.catalina" level="ERROR" />

</configuration>

